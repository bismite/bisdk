sudo: false
matrix:
  include:
    - os: osx
    - os: linux
      addons:
        apt:
          packages:
            - libsdl2-dev
            - libsdl2-image-dev
            - libglew-dev
before_install:
  - if [[ "$TRAVIS_OS_NAME" == "osx" ]]; then brew install mingw-w64; fi
script:
  - "sh setup.sh"
